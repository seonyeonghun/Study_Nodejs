doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title=title
    body 
        h1=message
        p=desc
        form(action="/email_post", name="emailForm", method="post")
            label(for="email") 이메일주소
            input(type="email", id="email", name="email", placeholder="type your email address here")
            br
            input(type="submit", id="send_email")
            button(class="ajaxsend", type="button") ajaxsend
        a(href="/") 처음으로    
        script(src="script/ajax.js")